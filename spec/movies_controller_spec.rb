require 'spec_helper'

describe MoviesController  do
	describe 'find movie with same director' do
		it 'should call the model method that performs a search for movies with the same director'
		it 'should select the search results for rendering'
		it 'should make the find movie with same director results available to that template'
	end
end